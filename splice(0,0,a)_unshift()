function prepend(arr, item) {
    var newArr=arr.slice(0);
    newArr.splice(0,0,item);
    return newArr;
}
//
function prepend(arr, item) {
    var newArr=arr.slice(0);
    newArr.unshift(item);
    return newArr;
}